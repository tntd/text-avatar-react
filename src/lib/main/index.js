import React, { useState, createContext } from 'react';
import { waveImages } from './constant';

import './index.less';

const Disk = (props) => {
	const { id, account, empStatus, nickname } = props;

	return (
		<div className="tntx-text-avatar-wrap">
			hi
		</div>
	);
};

export default Disk;
